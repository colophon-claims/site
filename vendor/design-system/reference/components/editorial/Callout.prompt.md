Where the report states its own boundaries. Every published report carries at least one `limitation` callout.

```jsx
<Callout kind="limitation" title="What this does not show">
  These results cover one task suite at one date. They do not establish that either configuration is better in general.
</Callout>
```