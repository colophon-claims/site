Closed-set chooser.

```jsx
<Select label="Execution mode" options={[{value:'network',label:'Network operators'},{value:'local',label:'Local runner'}]} hint="Local runs are self-reported, not observed by Colophon." />
```