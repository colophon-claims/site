Multi-select. Always give consequential options a `description`.

```jsx
<Checkbox label="Retain disagreement" description="Conflicted tasks stay conflicted instead of resolving by majority." defaultChecked />
```