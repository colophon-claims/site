The travelling artifact — README badges, docs headers, release notes.

```jsx
<ClaimBadge value="62.4% · 500 tasks" status="observed" href="https://colophon.press/r/harness-ab-2026-08" />
<ClaimBadge value="71.1% · 500 tasks" status="attested" href="…" />
```

Rules: the badge must resolve to the complete report; the value must carry its denominator; `conflicted` and `incomplete` are shown, not suppressed. Never render a checkmark or the word "verified".