Appears at the top of the report body and in the app header during a run.

```jsx
<MethodLock state="locked" digest="sha256:9f3c…a71b" timestamp="2026-08-02T14:20:11Z" detailHref="/r/hb-2026-08/method" />
```

Wording is fixed: "Method locked before execution" — not "verified", not "certified". An `amended` lock must always be published with its reason.